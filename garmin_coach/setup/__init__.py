"""First-run onboarding: connect the athlete's Garmin and Claude accounts from
the dashboard's landing page, so a non-coder can set the app up by double-clicking
it — no terminal, no API key. The AI runs on their own Claude subscription via the
local `claude` CLI (see `claude_auth`)."""
